version: '3.8'

# Lightweight mode now expects Neo4j to be installed and started on host machine.
# Keep this file as a placeholder to avoid accidental docker startup for database services.
services: {}
